# Completed tasks

Task definitions arrive here only through the same pull request that implements them. A task is considered complete only after that pull request passes required checks and is merged into `main`.
