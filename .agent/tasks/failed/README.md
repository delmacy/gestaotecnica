# Failed tasks

Move a task here only after the automatic pull request is intentionally abandoned or requires human redesign. Include the failure reason, relevant run or pull request, and the condition required before returning it to `ready/`.
