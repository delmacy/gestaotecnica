name: Agent Work Governance
on:
  pull_request:
    paths:
      - 'src/agent-work/**'
      - 'docs/agent-work/**'
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npx tsc --noEmit
      - run: npm run test:unit
