# MOD-01-008-module-manifest-schema-uniqueness - Add module manifest schema with uniqueness checks

Add a ModuleManifestSchema that validates the existing manifest shape and rejects duplicate values in actions, events, views, and dependencies.
