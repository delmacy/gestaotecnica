# MOD-01-009-registry-boundary-static-test - Add registry boundary static test

Add a static unit test that ensures src/platform/registry does not import UI/app layers such as src/app, src/components, or src/features UI modules.
