# MOD-01-010-capability-lifecycle-index-export - Export capability lifecycle helpers from capability index

Export the lifecycle helpers through the capability registry barrel so later sprints can consume the lifecycle contract from one stable entrypoint.
