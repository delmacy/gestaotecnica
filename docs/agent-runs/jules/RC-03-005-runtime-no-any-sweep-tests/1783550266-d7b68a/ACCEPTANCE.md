# Acceptance Criteria

- New test fails on `Record<string, any>`, `z.any()`, or `as any` in targeted runtime files.
- No production runtime files are changed.
- Required test command and build are reported in the PR.
