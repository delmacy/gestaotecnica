# Acceptance Criteria

- No `getProcessVersionById(db as any` remains in `runtime.service.ts`.
- No new explicit `any` or `as any` is introduced.
- `npm run build` passes or the PR explains a real unrelated blocker.
