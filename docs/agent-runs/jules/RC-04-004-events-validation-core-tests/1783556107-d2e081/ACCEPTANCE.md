# Acceptance Criteria

- New validation test covers valid minimal input.
- New validation test covers invalid workspace id and invalid event type.
- New validation test proves payload defaults to `{}`.
