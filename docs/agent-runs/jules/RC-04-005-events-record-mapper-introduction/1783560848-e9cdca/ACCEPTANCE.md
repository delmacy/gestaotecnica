# Acceptance Criteria

- `mapEventRow` is exported from `events.repository.ts`.
- Mapper test proves camelCase row maps to `EventRecord` fields.
- Existing repository return behavior is preserved except for adding the helper.
