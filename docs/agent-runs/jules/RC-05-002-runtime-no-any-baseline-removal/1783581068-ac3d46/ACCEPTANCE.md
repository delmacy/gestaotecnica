# Acceptance Criteria

- No baseline allowance remains for `src/features/workflow/runtime/events/events.actions.ts`.
- The sweep still forbids `Record<string, any>`, `z.any()`, and `as any`.
- The no-any sweep passes.
