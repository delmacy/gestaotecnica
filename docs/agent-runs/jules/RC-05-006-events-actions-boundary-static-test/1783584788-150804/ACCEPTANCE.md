# Acceptance Criteria

- Static test fails if `db as any` appears in `events.actions.ts`.
- Static test checks that `EventDb` is imported/used.
- Static test checks that `getTimelineForInstanceInputSchema` is referenced before the server call.
