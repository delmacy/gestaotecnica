# Acceptance Criteria

- Invalid `instanceId`, `entityId`, or `actorId` fails validation when provided.
- Valid optional UUID fields pass validation.
- Existing payload default behavior remains covered.
