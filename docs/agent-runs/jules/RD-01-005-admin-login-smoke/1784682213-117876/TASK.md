# RD-01-005-admin-login-smoke - Add admin login smoke under runtime-safe DB access

Create a repeatable smoke path for /auth/setup or /auth/login using real persisted auth data and runtime-safe DB credentials, not mocked auth or superuser access.
