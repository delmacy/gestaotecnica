# RD-01-007-schema-ci-gate - Gate CI on schemas and least-privilege DB access

Add or adjust validation to fail when auth/workspace/runtime schemas are absent or runtime env uses a superuser/owner credential.
