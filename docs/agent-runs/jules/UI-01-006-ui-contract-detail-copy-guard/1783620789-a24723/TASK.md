# UI-01-006-ui-contract-detail-copy-guard - Guard UI contract copy interactions

Make copy-to-clipboard interactions resilient when navigator.clipboard is unavailable, without changing the visual contract.
