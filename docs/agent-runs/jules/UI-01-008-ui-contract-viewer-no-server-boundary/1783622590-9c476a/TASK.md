# UI-01-008-ui-contract-viewer-no-server-boundary - Add UI contracts viewer boundary test

Add a static boundary test that the UI contracts viewer remains client/static and does not import server persistence or database modules.
