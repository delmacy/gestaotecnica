# Context

        OpenCode produced and published the current PR, but its autonomous delivery did not clear the final gate.
    Jules is now the last-resort correction executor. Work only on the existing branch and PR; do not create a duplicate.

    Fallback reason:
    Approved PR has completed failing required checks; returning to changes_requested.

    Original context:
    OpenCode produced and published the current PR, but its autonomous delivery did not clear the final gate.
Jules is now the last-resort correction executor. Work only on the existing branch and PR; do not create a duplicate.

Fallback reason:
Approved PR has completed failing required checks; returning to changes_requested.

Original context:
UX-NAV-03 UX/Full Stack Flow. Stack-based vertical product-slice task generated after governor review showed too little frontend/persistence impact.
