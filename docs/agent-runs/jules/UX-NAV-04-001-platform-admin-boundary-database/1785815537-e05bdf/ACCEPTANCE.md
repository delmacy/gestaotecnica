# Acceptance Criteria

- This task advances the authorized production journey: Builder identity, organization portfolio, and durable workspace selection.
- Stage outcome is complete and real: Database/persistence foundation.
- Render the authenticated Builder identity and organization-grouped workspace portfolio in /builder. Replace anonymous discovery and raw UUID labels; selecting any of the three created workspaces must persist server-side and survive reload across Builder, admin, and runtime routes.
- Evidence reuses or replaces the probe organization deliberately and follows records through database reload.
- Authentication is already proven; do not replace this task with another login-only report.
- No mock, anonymous userId, static fixture, localStorage-only state, or HTTP-200-without-state-change counts as success.
