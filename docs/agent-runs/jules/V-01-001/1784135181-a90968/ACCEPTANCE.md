# Acceptance Criteria

- CanonicalEventSchema has one authoritative definition/export path
- existing imports are updated without compatibility breakage
- focused tests or type checks cover the canonical export
