# Activation

Merge this reorganization only after repository variables, provider secrets, Actions pull-request permissions, auto-merge, and required branch checks are configured. The bootstrap documentation task should be the first manual run.
