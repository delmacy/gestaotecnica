# Auto-merge

The task runner enables squash auto-merge after opening the pull request. GitHub merges only after required repository checks and protection rules are satisfied.

Disable automatic merge for higher-risk tasks by adapting the runner to read task metadata before enabling it; the initial bootstrap task is intentionally low risk.
