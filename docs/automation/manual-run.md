# Manual execution

Run **OpenCode Task Runner** from the Actions panel. Leave `task_file` empty to select the first queued task, or provide an exact Markdown file name from `.agent/tasks/ready/`.

A manual run obeys the same concurrency and open-agent-PR safeguards as scheduled and event-driven runs.
