# Ownership

GitHub Actions owns queue selection, validation, Git operations, pull request creation, and auto-merge activation. OpenCode owns only working-tree implementation and repair within the task scope.
