# Provider secrets

The task runner exposes supported provider credentials to the OpenCode action. Only the credential matching `OPENCODE_MODEL` needs to be configured. Keep unused provider secrets unset and rotate active credentials according to the provider policy.
