# Public runner usage

The workflows use the standard `ubuntu-latest` GitHub-hosted runner. The repository remains public, while model-provider credentials stay in encrypted Actions secrets and session sharing is explicitly disabled.
