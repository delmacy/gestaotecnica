# Queue state transition

A task remains in `ready/` on `main` while implementation is in progress. The implementation pull request moves it to `completed/`; therefore the state transition becomes official only when the implementation itself is merged.
