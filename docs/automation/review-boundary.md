# Review boundary

The OpenCode implementation step is not the final reviewer. Repository checks and branch protection independently decide whether the pull request is eligible to merge. Human review can remain required for selected risk classes.
