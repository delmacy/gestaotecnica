# Task authoring

Keep each queued task small enough for one isolated pull request. Acceptance criteria must be observable, paths must be constrained, and validation commands must match the affected area.

Use zero-padded numeric prefixes to control order. Avoid placing multiple unrelated features in one task because the queue advances only after the current pull request is merged.
