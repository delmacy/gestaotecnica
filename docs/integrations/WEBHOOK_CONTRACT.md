# Webhook Contract

n8n é borda; agentes propõem; humanos aprovam; webhooks entram como sinais. Payload externo é autenticado, validado, normalizado, correlacionado e convertido em candidate/signal antes de entrar no domínio.
