# Modulo: Permissions / RBAC

## Configuracao

Use `/admin/permissions` para criar permissoes base e conceder permissoes por papel de negocio.

## Adaptacao por cliente

Papeis ficam em `business_role_definitions`; grants ficam em `role_permission_grants`. Ajuste por workspace sem codar regras locais no core.
