# Modulo: Queues / SLA

## Configuracao

Filas ficam em `workspace_queues`; itens em `queue_items`; politicas em `sla_policies`.

## Adaptacao por cliente

Configure filas, prazos por prioridade, responsaveis e regras de escalonamento.
