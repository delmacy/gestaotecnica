# Execution Model

Runtime permanece conceitual nesta etapa. Uma `process instance` referencia
versão publicada, avança por `state` e `action`, registra `event` imutável e
emite `receipt`. Não executa draft nem payload externo bruto.
