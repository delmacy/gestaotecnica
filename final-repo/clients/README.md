# Fleet Inventory

This directory contains (or references) the manifest of all client instances governed by this Control Plane.

## Structure
Each client should ideally have a dedicated subdirectory or a reference in a central registry.
Currently, clients are managed via `client-manifest.schema.json`.
