import React from "react";
import { FormBuilderStudio } from "@/components/builder/form-builder/FormBuilderStudio";

export const metadata = {
  title: "Form Builder | System Builder",
  description: "Criação visual e estruturação de formulários no System Builder",
};

export default function FormBuilderPage() {
  return <FormBuilderStudio />;
}
