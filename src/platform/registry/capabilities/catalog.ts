import { CAPABILITY_DOMAINS, CAPABILITY_GROUPS } from "./constants";

/**
 * Re-export constants for backward compatibility if needed,
 * although they should now be imported from constants.ts
 */
export { CAPABILITY_DOMAINS, CAPABILITY_GROUPS };
